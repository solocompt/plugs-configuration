============
Installation
============

At the command line::

    $ easy_install plugs-configuration

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv plugs-configuration
    $ pip install plugs-configuration
